language: node_js
sudo: required
before_install:
    - npm install -g node-gyp
node_js:
    - "8"
env:
    - CXX=g++-5
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
